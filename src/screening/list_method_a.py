"""Method A screening method (placeholder)"""

import pandas as pd


def list_method_a(db):
    """Get stock list using Method A

    Args:
        db (StockDatabase): Database instance

    Returns:
        pd.DataFrame: Empty DataFrame with columns ('code', 'name')
    """
    return pd.DataFrame(columns=['code', 'name'])
